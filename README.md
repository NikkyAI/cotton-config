cotton-config (root project)
============================

contains config loading and validation code

annotationns
============

needs to be added to `implementation` or otherwise compiled into the mod

annotationProcessor
===================

needs to be applied via `annotationProcessor` or `kap`
